import wollok.game.*
import niveles.*
import personajes.*
import extras.*

program bombIt {
	
	game.title("Bomb It")
	
	// Tamaño e imagen del fondo
	game.height(20)
	game.width(20)
	game.boardGround("fondo.png")
	
	//Inicia el primer nivel
	nivel1.iniciar()
	game.schedule(1000, {game.sound("soundtrack1.mp3")})
	game.start()
	
}